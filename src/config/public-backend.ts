// Public (safe-to-ship) backend connection values and identifiers.
// Firestore and Firebase auth configuration is handled via firebase-applet-config.json and /src/integrations/firebase/config.ts.
export const APP_NAME = "Qmark";
